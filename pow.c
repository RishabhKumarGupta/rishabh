#include <stdio.h>
#include<math.h>
void main()
{
       int a,n,c;
       printf(" enter the value:");
       scanf("%d",&a);
       printf(" enter the power:");
       scanf("%d",&n);
       	c=pow(a,n);
       	printf("%d is the value of given number",c);
       getch();
}
